name: Branch Up-to-Date Check
on:
  pull_request:
    branches:
      - dev

jobs:
  check-up-to-date:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify branch is up to date
        run: echo "✅ Branch is up to date with dev"
