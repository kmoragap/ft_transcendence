version: '3.8'

services:
  frontend:
    build: .
    container_name: ft_transcendence_frontend
    volumes:
      - .:/app
    ports:
      - "5173:5173"  # if you later add Vite or dev server
    command: npm run dev
